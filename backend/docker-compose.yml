
version: "3.9"

services:

  qdrant:
    image: qdrant/qdrant

    container_name: qdrant

    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"

    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data: